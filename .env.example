# Optional. If unset, the app runs fully in demo mode with mock AI responses.
# Get a key at https://openrouter.ai
OPENROUTER_API_KEY=

# Optional. Model id passed to OpenRouter. Defaults to a sensible free/cheap model if unset.
OPENROUTER_MODEL=openrouter/auto
